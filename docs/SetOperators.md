# Set Operators

Set operators allow queries to be combined.

## General Syntax

```sql
[ ( ] <query> [ ) ] ( INTERSECT  |  ( MINUS | EXCEPT ) | UNION [ ALL ] ) [ ( ] <query> [ ) ]
[ ORDER BY . . . ]
[ LIMIT . . . ]
```
