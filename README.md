# SQL - the language, the differences in implementations (Postgres PL SQL , SnowSQL, etc), architectural considerations, design patterns
Everything how to write high quality SQL which is optimized for performance
